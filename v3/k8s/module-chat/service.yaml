apiVersion: v1
kind: Service
metadata:
  name: module-chat-svc
  namespace: klosetlab
spec:
  type: ClusterIP
  selector:
    app: module-chat
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081
